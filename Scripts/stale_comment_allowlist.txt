# 注释承诺句扫描器的豁免表 —— 每行一条 `路径:行号`，`#` 之后是说明（可写在行尾）。
#
# ⚠️ **为什么要这个文件**：`Scripts/scan_stale_comments.sh` 的判据是
# 「标了未核实 / TODO / 已知限制… 就必须带日期」。它偶尔会命中**误报**
# （例如触发词出现在引号里的用户文案中），那种时候不应该是改代码，而是登记在这里。
#
# ⚠️ **行号会漂**：本表按 `路径:行号` 精确匹配，源文件一改行就对不上了 ⇒
#    豁免**静默失效**（门槛重新变红）。看到「明明登记过还红」，第一件事是查行号。
#
# 当前为空 —— 说明**没有一处需要豁免**。别为了「备着」往里塞条目：
# 空表本身就是「判据干净」的证据，塞了就失去了这个信号。
